{{define "content"}}
<div class="container">
	<div class="row">
		<div class="jumbotron">
			<h1>Er is een fout opgetreden</h1>
			<p>Er is iets misgegaan, het systeem gaf de volgende fout:</p>
			<pre>{{.SystemError}}</pre>			
		</div>
	</div>
</div>
{{end}}
